---
"@loglayer/transport-central": patch
---

Initial release of `@loglayer/transport-central`, a LogLayer transport for sending logs to the Central log aggregation server. Extends `HttpTransport` for built-in batching, retries, compression, and rate limiting support.
