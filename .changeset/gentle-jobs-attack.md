---
"@loglayer/shared": patch
---

Add optional disposable method to the `IContextManager` and `ILogLevelManager` interfaces. They've always supported it, but it was not explicitly noted for the interface.
